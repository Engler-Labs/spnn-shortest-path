{
  "subject": "(c, lambda) enrichment grid -- terminal-bias (b_st) provenance",
  "claims": ["SW-ENRICH", "SW-EVENTS"],
  "augmented": false,
  "b_st_named": false,
  "b_st_resolved": "n/a -- the grid is compiled UNAUGMENTED, so the auxiliary-terminal bias b_st is never read on this code path",
  "st_mode": "periodic",
  "inhib_kind": "off",
  "design": {
    "n_graphs": 8, "depth_constraint": "L>=8", "N": 60, "p": 0.05,
    "n_seeds": 3, "steps_per_run": 10000, "D": 4.0
  },
  "conclusion": "The (c,lambda) enrichment grid is compiled without the S/T augmentation, so the auxiliary-terminal bias b_st is never consulted and its value is irrelevant to the run. The 'terminal-bias never audited' concern is therefore moot for this harness. Had b_st been on the code path it would carry the anchored default 0.0, which is the correct published convention (the discrimination margin is b_st-independent under the aux-admitted competitor convention).",
  "label": "pinned",
  "label_reason": "The b_st concern is resolved (moot on the DEFAULT unaugmented path). Pinned, not reproduces, because the published enrichment digits (<=1.1 / 33.6 / 229 at c=1,2,3 along lambda=0) are not regenerated by any harness in the release.",
  "augmented_rerun_verdict": "The (c,lambda) sweep was RE-RUN augmented at the settled convention b_S=b_k (see enrichment.csv). It gives enrichment 0 / 198 / 1008 at c=1,2,3 (density**(L+2) baseline) or 0 / 12 / 80 (original-hop baseline). This matches NEITHER the printed 'construction' digits (<=1.1 / 33.6 / 229) NOR the unaugmented 'variant' digits (0.88 / 41.2 / 330). The printed and variant digits are the same order of magnitude (a population difference); augmenting moves them the other way. So section V-D's 'measured on the construction' attribution is UNSUPPORTED and needs correcting, not merely re-populating. The qualitative c-is-the-lever point holds on both.",
  "generated_date": "2026-08-18"
}
